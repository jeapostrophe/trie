(module info (lib "infotab.ss" "setup")
  (define name "trie maps")
  (define homepage "http://jay.makeoutcity.com/#trie")
  (define blurb
    (list "Finite maps as big-endian binary tries"))
  (define primary-file "fmap.ss")
  (define doc.txt "doc.txt")
  (define categories `(datastructures)))